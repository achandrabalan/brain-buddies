import React from 'react';

const LandingContent = () => {
  return (
    <div className="flex flex-col items-center justify-center w-full h-[75vh] bg-brain">
      hello world{' '}
    </div>
  );
};

export default LandingContent;

// a picture of leaderboard

// a picture of stats / profile / acheivements
// a picture of groupchat conversation

// confirmation modal stuff on hamburger click

// leaderboard modal design

// default profile pics for users (ai generated)
